# Seed Project — AI 行为约束规则

## 技术栈（不可更改）
- 前端: Vue 3.4+, Vite 5+, TypeScript 5.x, TailwindCSS 3.x, Pinia 2.x
- 后端: Python 3.11+, FastAPI 0.111+, SQLAlchemy 2.0+, Pydantic v2
- AI: Claude Agent SDK (Anthropic), OpenAI Python SDK 1.x
- 数据库: PostgreSQL 16, Redis 7.x
- 部署: Vercel (前端), Railway (后端)

## 目录结构（不可更改）
遵守 `docs/spec.md` 第三章定义的目录结构，不得创建新的一级目录。
- 所有后端代码在 `backend/app/` 下
- 所有前端代码在 `frontend/src/` 下
- 所有文档在 `docs/` 下
- 题库在 `backend/app/data/questions.json`

## 命名规范
- Python 文件/函数/变量: snake_case
- TypeScript 文件: kebab-case
- Vue 组件: PascalCase
- 接口/类型: PascalCase
- SQLAlchemy 模型: PascalCase (CamelCase)
- 数据库表名: snake_case 复数形式
- 测试文件: test_*.py (后端), *.spec.ts (前端)

## API 规范
- 所有接口返回统一格式: `{"code": 0, "data": {}, "msg": "success"}`
- 错误码严格使用 contract.md 定义的值
- 响应模型定义在 `schemas/` 下，使用 Pydantic v2
- 业务逻辑在 `services/` 下，接口在 `endpoints/` 下
- 鉴权通过 Depends 注入，不重复实现

## AI Agent 规范
- 使用 Claude Agent SDK，不裸调 API
- 每个 Tool 独立一个文件，放在 `agents/tools/` 下
- Tool 的 docstring 即 prompt，必须包含: 功能、输入、输出、约束
- Agent 不出题，只选题（从 `data/questions.json` 选）
- 计分在沙箱中执行，不依赖 LLM
- 所有 Agent 调用必须支持重试（2次，指数退避）

## 前端规范
- 组件拆分: 页面级在 views/，通用在 components/common/，布局在 components/layout/
- API 调用统一通过 `api/client.ts` 的 axios 实例
- 状态: Pinia stores，每个模块一个 store
- 移动优先: 375px 基准宽度，使用 Tailwind 响应式断点
- 聊天式 UI: 消息气泡样式统一，选项按钮统一组件

## Git 规范
- 提交信息: feat: / fix: / chore: / docs: / test:
- 分支: feature/m{编号}-{描述}
- 每个模块完成后打 tag: v0.{模块号}
- 不做 force push

## 质量标准
- 后端: 所有接口 ≥80% 测试覆盖，pytest + httpx
- 前端: 关键组件有 vitest 测试
- 无 any 类型（前端 TS）
- 所有函数有类型注解（后端 Python）
- Pydantic 校验所有请求/响应

## 禁止事项
- 不得使用 npm/yarn/pnpm 之外的包管理器
- 不得在代码中硬编码密钥
- 不得创建 spec/contract 未定义的接口
- 不得跳过 .cursorrules 和 contract.md 的约束
- 不得使用 Coze 或任何低代码平台
